/// @description Insert description here
// You can write your code in this editor
if(invincible){
	
} else {
	pHealth = pHealth - 1;
	
	with (obj_playerPart2)
	{
	// Change the player's color for a moment
	image_blend = make_color_rgb(255, 0, 0);
	// Change their color back to normal
	alarm[1] = 7
	}
}